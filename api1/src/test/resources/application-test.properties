# Avoid connecting to the real database.
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Disable Flyway in the test profile.
spring.flyway.enabled=false

# Avoid connecting to Kafka.
spring.kafka.bootstrap-servers=embedded-kafka:1234  # qualquer valor dummy funciona

# Avoid connecting to Redis.
spring.redis.host=localhost
spring.redis.port=0

# Disable security
spring.security.enabled=false
